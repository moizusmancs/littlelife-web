/**
 * Flood Intelligence — flood predictions, hazard zones.
 * Backend doc: supporting-material/api/03-flood-intelligence.md (built).
 * Filled in during FRONTEND_IMPLEMENTATION_PLAN.md Phase 3 — read §2.1/§2.3 of that plan
 * before wiring the map overlay call, the flood-visualization rendering is corrected there
 * (grid-box probability rendering from supporting-material/map-preview/, not the design
 * mockup's blob-polygon guess).
 */
export {}

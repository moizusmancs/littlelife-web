/**
 * Relief Operations (core) — aid requests, donation campaigns + donations, missing persons +
 * sightings.
 * Backend doc: supporting-material/api/09-relief-operations.md (built).
 * Filled in during FRONTEND_IMPLEMENTATION_PLAN.md Phase 6.
 *
 * NOTE: field observations and community feedback (M16 FE-10/11) have NO route in this doc
 * despite their tables existing in the Phase 9 migration — confirmed by grep, not an oversight.
 * Those two belong in src/mocks/handlers (Phase 7), not here, until the backend actually ships them.
 */
export {}

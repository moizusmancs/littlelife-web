/**
 * Identity — auth, NGO lifecycle, admin account management.
 * Backend doc: supporting-material/api/00-identity.md (built, Phase 0 of the backend roadmap).
 * Filled in during FRONTEND_IMPLEMENTATION_PLAN.md Phase 1 — kept as a typed-but-empty module
 * until then so the service-layer file layout exists from the start (see Phase 0 §3).
 */
export {}

import type { HttpHandler } from 'msw'

/**
 * Reporting & Audit — backend Phase 14, not built (FRONTEND_IMPLEMENTATION_PLAN.md §1).
 * Target contract: WEB_DESIGN_PLAN.md §6.3/§6.4 Reports, Audit Log, Analytics. Filled in
 * during Phase 7 — remember to mock the async "Generating…" pending state for report
 * creation, not just the final result.
 */
export const reportingHandlers: HttpHandler[] = []

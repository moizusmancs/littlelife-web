import type { HttpHandler } from 'msw'

/**
 * Relief Ops follow-up: escalations — backend Phase 13, not built
 * (FRONTEND_IMPLEMENTATION_PLAN.md §1). Target contract: WEB_DESIGN_PLAN.md §6.3/§6.4
 * Coordination / Escalations tab. Filled in during Phase 7.
 */
export const escalationsHandlers: HttpHandler[] = []

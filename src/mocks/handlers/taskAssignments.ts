import type { HttpHandler } from 'msw'

/**
 * Relief Ops follow-up: task assignments — backend Phase 11, not built
 * (FRONTEND_IMPLEMENTATION_PLAN.md §1). Target contract: WEB_DESIGN_PLAN.md §6.3/§6.4 Tasks
 * (Board/List). Filled in during Phase 7.
 */
export const taskAssignmentsHandlers: HttpHandler[] = []

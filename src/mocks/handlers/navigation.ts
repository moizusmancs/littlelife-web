import type { HttpHandler } from 'msw'

/**
 * Navigation (`internal/navigation`) — not built (FRONTEND_IMPLEMENTATION_PLAN.md §1).
 * Target contract: WEB_DESIGN_PLAN.md §6.2 Safe Route Navigation. Filled in during Phase 7.
 */
export const navigationHandlers: HttpHandler[] = []
